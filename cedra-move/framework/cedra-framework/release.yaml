---
remote_endpoint: http://localhost:8080/
# replace with below for actual release, compat test needs concrete URL above:
# remote_endpoint: ~
name: "2.0.0"
proposals:
  - name: whitelist
    metadata:
      title: "Multi-step proposal to upgrade mainnet framework, version v2.0.0"
      description: "This includes changes in https://github.com/aptos-labs/aptos-core/releases/tag/aptos-node-vX.YY.Z"
    execution_mode: MultiStep
    update_sequence:
      - Gas:
          new: current
          update_sequence:
      - FeatureFlag:
          enabled:
            - fee_v2
          disabled: []
      - Framework:
          bytecode_version: 7
          git_hash: ~
